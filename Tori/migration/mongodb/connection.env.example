# Preparation only: these values are NOT read by the current bot.
# Keep real credentials in private configuration, never in this template.
MONGODB_URI=
MONGODB_DATABASE=tori
# Explicit guard for the future manual mongosh preparation step.
TORI_MONGO_PREPARE=NO
