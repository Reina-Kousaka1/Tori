# For local Java runs outside Docker Compose. Main Tori reads both variables.
# Keep real credentials in private .env, never in this template.
MONGODB_URI=mongodb://tori:REPLACE_WITH_PASSWORD@127.0.0.1:27018/?authSource=admin
MONGODB_DATABASE=tori_main
